import React, { useEffect, useState, useContext } from "react";
import Card from "components/Card";

const Home = () => {
  return (
    <Card elevationLevel={4}>
      <span style={{ fontFamily: "Source Sans Pro" }}>Hommer</span>
    </Card>
  );
};

export default Home;
