import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import Button from "../components/Button";
import {ButtonVariants,ButtonIntent} from "../components/Button/style.tsx";

<Meta title="Button" />

Here's some _markdown_!;

# Preview

<Preview>
  <Story name="Primary">
    <Button variant={'Primary'} intent={'Neutral'} onClick={action("Default button clicked")}>Default</Button>
    </Story>
  <Story name="Secondary">
     <Button variant={'Secondary'} intent={'Success'} onClick={action("Default button clicked")}>Default</Button>
    </Story>
  <Story name="Terciary">
     <Button variant={'Terciary'} intent={'Danger'} onClick={action("Default button clicked")}>Default</Button>
  
   </Story>
  <Story name="Quartiary">
     <Button variant={'Quartiary'} intent={'Neutral'} onClick={action("Default button clicked")}>Default</Button>
    
   </Story>
</Preview>

# Props

<Props of={Button} />

## Variants

  <div> {
    Object.values(ButtonVariants).filter(x => typeof x === 'string').map(variant => (<pre>{variant}</pre>))
  } </div>

## Intents

  <div> {
    Object.values(ButtonIntent).filter(x => typeof x === 'string').map(intent => (<pre>{intent}</pre>))
  } </div>
 